# Set environment variables for cron
PATH=/usr/local/bin:/usr/bin:/bin
SHELL=/bin/bash

# Run the full pipeline (transfer, index, preprocess, upload) at 06:00 UTC daily
0 6 * * * root /app/cron-wrapper.sh

# Empty line required at end of cron file
