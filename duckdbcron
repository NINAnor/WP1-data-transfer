# Set the envi variables for the cron job
RCLONE_CONFIG=/run/secrets/tabmon_rclone
GOOGLE_APPLICATION_CREDENTIALS=/run/secrets/gcs_key

# Run the moving and preprocessing everyday at midnight
1 0 * * * /app/cron-wrapper.sh # run the process at midnight and 1 minute every day

# Add an line at the end of the cron file